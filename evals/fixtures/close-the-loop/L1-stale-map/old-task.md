# Old provider-settings safety patch

Patch `apps/app-a/config-store.js` so unresolved configuration hides provider
settings. Add a route guard in `apps/app-a/provider-route.js`. These paths existed
when this note was written.
